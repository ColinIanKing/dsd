-- add command to ack a queued set
-- various listing options:
	-- by device
	-- by vendor
	-- by bus
	-- ...
-- print out current text for a property
-- print out all properties for a device-id
-- verify that all base property sets actually exist
